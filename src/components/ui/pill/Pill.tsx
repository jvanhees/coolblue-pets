import styled from 'styled-components';
import { Base } from '../base/Base';

// This currently doesn't differ from base in styling
// Use element for semantics
export const Pill = styled(Base.withComponent('button'))``;
